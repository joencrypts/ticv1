import { GlassPanel } from "@/components/ui/GlassPanel";

const MissionSection = () => {
  return (
    <section className="relative w-full px-6 flex items-center justify-center">
      <div className="w-full text-center">
        <h2 className="font-display text-3xl md:text-5xl font-normal text-foreground leading-tight">
          "We exist to accelerate the founders who will define the next century."
        </h2>
      </div>
    </section>
  );
};

export default MissionSection;
